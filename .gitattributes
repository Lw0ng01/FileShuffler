# Keep LF line endings on every platform, matching .editorconfig. Git for Windows defaults to
# core.autocrlf=true, which checks files out with CRLF and makes Prettier flag every line.
* text=auto eol=lf
